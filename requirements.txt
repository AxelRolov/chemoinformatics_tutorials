# Local/offline installation of the full course stack.
# On Google Colab you do NOT need this file: each notebook installs what it needs in its setup cell.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# core
numpy>=1.26
pandas>=2.0
scipy
matplotlib
seaborn
plotly
tqdm
requests
jupyterlab

# chemistry
rdkit>=2024.3
selfies
mols2grid
py3Dmol
chembl_webresource_client
pubchempy
chembl_structure_pipeline

# machine learning
scikit-learn>=1.4
xgboost
umap-learn
joblib

# deep learning (session 07-08); install a CUDA build of torch if you have a GPU
torch>=2.2
torch_geometric
transformers

# molecular modeling, docking and MD (sessions 09-11)
openmm>=8.1
mdanalysis
pdbfixer
# vina has wheels only up to Python 3.12; on 3.13+ pip builds it from source, which needs SWIG and the Boost headers
# (Debian/Ubuntu: apt-get install swig libboost-dev libboost-thread-dev libboost-serialization-dev
#  libboost-filesystem-dev libboost-program-options-dev; conda: conda install -c conda-forge vina instead)
vina
meeko
prolif
openbabel-wheel

# agents (session 08)
smolagents[litellm]

# notebook build system (instructors)
jupytext
nbformat
nbclient
